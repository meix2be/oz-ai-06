// 논리연산자(Logical Operator)
// 여러 개의 참/거짓 조건을 조합해서 판별 -> 결과는 Boolean

// and 조건: &&
// or 조건: ||
// not 연산자: 참거짓을 반대로

let age = 20;
let hasId = true;

console.log(age >= 19 && hasId); //true
console.log(age < 19 || hasId); //true
console.log(!hasId); //false